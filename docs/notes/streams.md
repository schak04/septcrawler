# Streams

A stream represents a continuous sequence of data flowing between a program and an external source or destination.  
Instead of reading an entire file or network packet into memory all at once, streams allow a program to
read or write data sequentially, one chunk at a time.

- Input Streams: Used to move data into the program from an external source.
- Output Streams: Used to move data out of the program to an external destination.
